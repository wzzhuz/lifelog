*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build/
/app/release/
/captures
.externalNativeBuild
.cxx
local.properties
*.jks
*.keystore
keystore.properties
# 注意：app/debug.keystore 必须提交进仓库，
# 这样每次 CI 编译出的 APK 签名才一致，能直接覆盖安装。
!app/debug.keystore
