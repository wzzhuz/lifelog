# 任务清单

## 1. 建立目录结构

- [x] 1.1 创建 `openspec/specs/` 下 7 个模块目录
- [x] 1.2 创建 `openspec/changes/archive/`

## 2. 编写核心 spec

- [x] 2.1 `status-calculation` —— 状态计算的三级回退与边界
- [x] 2.2 `record-capture` —— 四个摩擦层级与倒填日期
- [x] 2.3 `home-widget` —— 两种形态与 Glance 限制
- [x] 2.4 `data-persistence` —— 原子写入与滚动备份

## 3. 编写次要 spec

- [x] 3.1 `event-management`
- [x] 3.2 `timeline-review`
- [x] 3.3 `backup-restore`

## 4. 编写约定文件

- [x] 4.1 `project.md` —— 项目级约束与已确认不做清单
- [x] 4.2 `AGENTS.md` —— AI 行为指引与流程说明

## 5. 归档

- [x] 5.1 创建本归档记录
- [x] 5.2 推送到仓库

## 未做（有意推迟）

- [ ] 界面视觉 spec —— 等实际调整 UI 时补
- [ ] 预置模板清单 spec —— 等模板内容变更时补
- [ ] 安装 openspec CLI —— 需 Node 20.19+，且当前工具不支持斜杠命令
